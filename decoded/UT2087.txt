     H OPTION(*SRCSTMT : *NODEBUGIO) CVTOPT(*NODATETIME) ALWNULL(*USRCTL)
     H DFTACTGRP(*NO) ACTGRP(*CALLER) BNDDIR('QC2LE' : 'UTBIND')
      // -------------------------------------------------------------------------------------------
      //
      // Program  : UT2087 - Stage Remote Variable Valid Values
      // Project  : Infrastructure - Configuration
      // Author.  : G.Neale                          Date. . : 11/28/2011
      //
      // Narrative: Create and load UTCFGVVL from remote system
      //
      // -------------------------------------------------------------------------------------------
      // Modification History:
      //--------------------------------------------------------------------------------------------
      //
      // -------------------------------------------------------------------------------------------

      /Copy QCPYSRC,PGSDS

      *--------------------------------------------------------------------
      * Define entry prototype and procedure interface
      *--------------------------------------------------------------------
     D EntryParms      PR                  EXTPGM('UT2087')
     D                               20A                                        Remote Server

     D EntryParms      PI
     D   @Remote                     20A                                        Remote Server

      /Copy QCPYSRC,UTBIND_PR

      *--------------------------------------------------------------------
      * Define variables
      *--------------------------------------------------------------------
     D  @MsgID         S              7A
     D  @Local         S             20A                                        Local Server

     D  Vvl_ID         S              9B 0
     D  Vdf_ID         S              9B 0
     D  Vvl_Value      S            128A
     D  Vvl_Desc       S             50A
     D  Vvl_Info       S           2048A

     **********************************************************************
     **        M A I N L I N E    C O N T R O L    S E C T I O N
     **********************************************************************
      /FREE

       // Set current server name

        Exec Sql
           Select   Current Server
           Into     :@Local
           From     Sysibm/SysDummy1;

       // Drop local temp table

       EXEC SQL SET CONNECTION :@Local;

       Exec SQL Drop Table QTEMP/TEMP_VVL;

       // Create local temp table

       Exec SQL Create Table QTEMP/TEMP_VVL Like UTCFGVVL;

       // Create SQL package prior to execution

       RunCommand('CRTSQLPKG PGM(' + %trim(#PS.ProgramName) +
                    ') RDB(' + %trim(@Remote) + ')');

       // Define and open cursor

       EXEC SQL DECLARE RemoteTable CURSOR FOR
          SELECT   VVL_ID,
                   VDF_ID,
                   TRIM(VVL_VALUE),
                   VVL_DESC,
                   TRIM(VVL_INFO)
           FROM    UTCFGVVL;

       EXEC SQL OPEN RemoteTable;

       Dou SQLCod <> *zero;
          EXEC SQL FETCH RemoteTable INTO
                   :Vvl_ID, :Vdf_ID, :Vvl_Value, :Vvl_Desc, :Vvl_Info;
          If SQLCod = *zero;
             EXEC SQL SET CONNECTION :@Local;
             Exec SQL
                Insert   into TEMP_VVL
                Values   (
                         :Vvl_ID,
                         :Vdf_ID,
                         TRIM(:Vvl_Value),
                         :Vvl_Desc,
                         TRIM(:Vvl_Info));
              EXEC SQL SET CONNECTION :@Remote;
           Endif;
       Enddo;

       EXEC SQL CLOSE RemoteTable;

       // Disconnect from FCB and repoint to DEV

       EXEC SQL DISCONNECT :@Remote;
       EXEC SQL SET CONNECTION :@Local;
       If  SQLCOD = -843;
           EXEC SQL CONNECT RESET;
       Endif;

       // End of program

       *INLR = *On;

      /end-free
